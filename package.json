{
  "name": "tamil-nadu-diet-plan",
  "private": true,
  "version": "0.1.0",
  "description": "Health & Fitness SaaS monorepo scaffold",
  "engines": {
    "node": "20.x"
  },
  "workspaces": [
    "apps/api",
    "apps/web",
    "packages/shared"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "turbo": "^2.1.3",
    "typescript": "^5.6.3"
  }
}
