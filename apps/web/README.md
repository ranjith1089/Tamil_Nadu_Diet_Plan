# Web App (Next.js)

Initial scaffold for the web app. Full UI routes and feature modules will be added from Step 6 onward.

## Deploy on Vercel
- Preferred: set Vercel project **Root Directory** to `apps/web`.
- Ensure `NEXT_PUBLIC_API_BASE_URL` is configured in Vercel environment variables.
